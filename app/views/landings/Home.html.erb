<% provide(:title, "ホーム") %>
<ul class="nav nav-tabs mt-1" style="margin-left: -12px; margin-right: -12px">
  <li class="nav-item">
    <% if @page == 1 %>
        <a class="nav-link active" aria-current="page" href="<%= root_path %>">無人販売一覧</a>
    <% else %>
        <a class="nav-link" aria-current="page" href="<%= root_path %>">無人販売一覧</a>
    <% end %>
  </li>
  <li class="nav-item">
    <% if @page == 2 %>
        <a class="nav-link active" aria-current="page" href="<%= showmap_path %>" >🌏地図</a>
    <% else %>
        <a class="nav-link" aria-current="page" href="<%= showmap_path %>" >🌏地図</a>
    <% end %>
  </li>
</ul>
<div class="row bg-white border-start border-end border-bottom rounded-bottom rounded-5">
    <div class="col-12">
        <% if @page == 1 %>
            <%= render 'index' %>
        <% else %>
            <%= render 'map' %>
        <% end %>
    </div>
</div>